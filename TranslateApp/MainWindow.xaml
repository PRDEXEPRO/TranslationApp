<Window x:Class="TranslateApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Çeviri Uygulaması" Height="650" Width="950"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Menü Çubuğu -->
        <Menu Grid.Row="0" Background="#E0E0E0" Height="30">
            <MenuItem Header="Hakkında">
                <MenuItem Header="Bilgi" Click="SettingsMenuItem_Click"/>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Başlık -->
            <TextBlock Grid.Row="0" 
                       Text="🌍 Çeviri Uygulaması" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"
                       Foreground="#333"/>

        <!-- Kaynak Dil Bölümü -->
        <GroupBox Grid.Row="1" 
                  Header="Kaynak Metin" 
                  FontSize="16" 
                  FontWeight="SemiBold"
                  Margin="0,0,0,10"
                  Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <ComboBox x:Name="SourceLanguageComboBox" 
                          Grid.Row="0" 
                          Height="35" 
                          Margin="0,0,0,10"
                          FontSize="14"
                          SelectionChanged="SourceLanguageComboBox_SelectionChanged"/>
                
                <TextBox x:Name="SourceTextBox" 
                         Grid.Row="1" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto"
                         FontSize="14"
                         Padding="10"
                         BorderBrush="#DDD"
                         BorderThickness="1"
                         Background="White"/>
            </Grid>
        </GroupBox>

        <!-- Çeviri Butonları -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,10">
            <Button x:Name="TranslateButton" 
                    Content="🔄 Çevir" 
                    Height="45" 
                    Width="150"
                    FontSize="16" 
                    FontWeight="Bold"
                    Margin="5,0"
                    Click="TranslateButton_Click"
                    Background="#2196F3"
                    Foreground="White">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button x:Name="SwapButton" 
                    Content="⇄ Dilleri Değiştir" 
                    Height="45" 
                    Width="180"
                    FontSize="16" 
                    FontWeight="Bold"
                    Margin="5,0"
                    Click="SwapButton_Click"
                    Background="#FF9800"
                    Foreground="White"
                    ToolTip="Kaynak ve hedef dilleri değiştirir ve çeviriyi tersine çevirir">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F57C00"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <!-- Hedef Dil Bölümü -->
        <GroupBox Grid.Row="3" 
                  Header="Çeviri Sonucu" 
                  FontSize="16" 
                  FontWeight="SemiBold"
                  Margin="0,10,0,0"
                  Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <ComboBox x:Name="TargetLanguageComboBox" 
                          Grid.Row="0" 
                          Height="35" 
                          Margin="0,0,0,10"
                          FontSize="14"
                          SelectionChanged="TargetLanguageComboBox_SelectionChanged"/>
                
                <TextBox x:Name="TargetTextBox" 
                         Grid.Row="1" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True" 
                         IsReadOnly="True"
                         VerticalScrollBarVisibility="Auto"
                         FontSize="14"
                         Padding="10"
                         BorderBrush="#DDD"
                         BorderThickness="1"
                         Background="#F9F9F9"/>
            </Grid>
        </GroupBox>

        <!-- Durum Çubuğu -->
        <StatusBar Grid.Row="4" 
                   Height="30" 
                   Margin="0,10,0,0"
                   Background="#E0E0E0">
            <StatusBarItem>
                <TextBlock x:Name="StatusTextBlock" 
                           Text="Hazır" 
                           FontSize="12"
                           Foreground="#666"/>
            </StatusBarItem>
        </StatusBar>
        </Grid>
    </Grid>
</Window>

