<Window x:Class="TranslateApp.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="API Ayarları" Height="250" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Microsoft Translator API Ayarları" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   Margin="0,0,0,20"
                   Foreground="#333"/>

        <TextBlock Grid.Row="1" 
                   Text="API Anahtarı:" 
                   FontSize="14" 
                   Margin="0,0,0,5"
                   Foreground="#666"/>

        <PasswordBox x:Name="ApiKeyPasswordBox" 
                     Grid.Row="2" 
                     Height="35" 
                     FontSize="14"
                     Padding="10"
                     Margin="0,0,0,10"
                     BorderBrush="#DDD"
                     BorderThickness="1"
                     Background="White"/>

        <TextBlock Grid.Row="3" 
                   TextWrapping="Wrap" 
                   FontSize="11" 
                   Foreground="#999"
                   Margin="0,0,0,20">
            <Run Text="API anahtarınızı almak için: "/>
            <Hyperlink NavigateUri="https://portal.azure.com/#create/Microsoft.CognitiveServicesTextTranslation" 
                       RequestNavigate="Hyperlink_RequestNavigate">
                Azure Portal
            </Hyperlink>
            <Run Text=" adresini ziyaret edin."/>
        </TextBlock>

        <StackPanel Grid.Row="5" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Kaydet" 
                    Width="100" 
                    Height="35"
                    Click="SaveButton_Click"
                    Background="#4CAF50"
                    Margin="0,0,10,0"/>
            <Button Content="İptal" 
                    Width="100" 
                    Height="35"
                    Click="CancelButton_Click"
                    Background="#757575"/>
        </StackPanel>
    </Grid>
</Window>

